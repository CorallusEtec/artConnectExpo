import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    postActionContainer: {
    flexDirection: "row",
    alignItems: "center",
    gap: 6,
  },

  actionInsight: {
    fontWeight: "500",
  },
})