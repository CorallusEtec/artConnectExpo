import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  container: {
    flex: 1,
    width: "100%",
  },
  postFlatContainer: {
    padding: 10,
  },
  postContentContainer: {
    gap: 10,
  },
});
