import { StyleSheet } from "react-native";

/**
 * Estilos do BannerLogo
 *
 * Proporção do banner para às telas: W / H -> 100% / 25% -> 1 / 0.25 = 4
 */

export const style = StyleSheet.create({
  banner: {
    width: "100%",
    resizeMode: "contain",
  },
});
