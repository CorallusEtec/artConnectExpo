import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  actionContainer: {
    flexDirection: "row",
    alignItems: "center",
  },
  actionInsight: {
    fontSize: 17,
    fontWeight: "600",
  },
  actionIcon: {},
  cardActionContainer: {
    flexDirection: "row",
    justifyContent: "space-between",
  },
});
