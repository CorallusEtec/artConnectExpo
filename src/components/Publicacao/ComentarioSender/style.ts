import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  input: {
    width: "100%",
    fontSize: 15,
  },
  actionContainer: {
    flexDirection: "row",
    justifyContent: "flex-end",
  },
});
