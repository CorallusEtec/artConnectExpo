import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  headerContainer: {
    padding: 15,
    flexDirection: "row",
    justifyContent: "space-between",
  },
  headerTitle: {
    fontWeight: "medium",
  },
});
