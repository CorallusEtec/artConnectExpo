import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  alertContent: {
    flexDirection: "row",
    justifyContent: "space-between",
  },
  alertText: {
    color: "white",
  },
});
