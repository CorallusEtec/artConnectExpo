import { gStyles } from "@/style/gStyle";
import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  active: {
    color: gStyles.azul[400],
  },
  normal: {
    color: gStyles.cinza[500],
  },
});
