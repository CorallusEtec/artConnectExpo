import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "white",
    gap: 10,
    paddingTop: 120
  },
  view1: {
    gap: 20,
    alignItems: "center",
  },
});
