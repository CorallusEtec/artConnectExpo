import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  container: {
    flex: 1,
    padding: 16,
    backgroundColor: "#FFF",
  },
  button: {
    marginTop: 16,
    borderRadius: 8,
    paddingVertical: 4,
  },
});
