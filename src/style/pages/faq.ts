import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  container: {
    flex: 1,
  },
  subtitleContainer: {
    paddingVertical: 10,
    paddingHorizontal: 20,
  },
  scrollContent: {
    paddingBottom: 32,
  },
});